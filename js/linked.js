(function(){ 
  
  /* var img = $(".img");
	var width = $("#leftcol").width();
	var ratio = img.height()/img.width();
  var imgHeight = img.height()/ratio;

	function resize() { 
    img.css({"width" : width, "height" : imgHeight});
} */

})();